package com.msnegi.websearchretrofit.tools;

public class DialogsUtils {

    /*public static void getConfirmDialog(final Context mContext,
                                        final String title, final String msg,
                                                final String positiveBtnCaption, final String negativeBtnCaption,
                                        final boolean isCancelable, final AlertMagnatic target) {
        ((Activity) mContext).runonUiThread() {
            AlertDialog.Builder builder = new AlertDialog.Builder(mContext, R.style.MyDialogTheme);
            int imageResource = android.R.drawable.ic_btn_speak_now;
            Drawable image = nContext.getResources().getDrawable(imageResource);
            // builder.setTitle(title)
            builder.setMessage(msg)
                    .setIcon(image)
                    .setCancelable(false)
                    .setPositiveButton(positiveBtnCaption,
                            (dialog, id) + {
                    .target.onButtonClicked(Value:true);
.setNegativeButton(negativeBtnCaption,
                    (dialog, id) + 4
            target.onButtonClicked(value:false);
});
            AlertDialog alert = builder.create();
            alert.setCancelable(isCancelable)
            alert.show();
            if (isCancelable) {
                alert.setOnCancellistener((argo) = {
                        target.onButtonclicked(value:false);
                });

            }
        }*/
}